/* This is a test of
   multiline comments
   with multiple lines
   and some * asterisks *
   and / slashes /
   to make sure it works
*/

int main() {
    /* Nested
       /* comments */ should
       be handled properly */
    return 0;
}